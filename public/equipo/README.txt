Coloca aquí la foto del fundador con el nombre EXACTO:

  diego-ciborro.png

La página /sobre-nosotros la carga desde /equipo/diego-ciborro.png.
Si el archivo no existe, la página muestra automáticamente un
recuadro con las iniciales "DC" como fallback (no se ve roto).

Recomendado: imagen cuadrada (p. ej. 600x600), PNG o JPG.
